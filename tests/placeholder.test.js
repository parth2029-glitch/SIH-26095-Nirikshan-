import { test } from 'node:test';
import assert from 'node:assert/strict';

// Placeholder so CI has something to run before §15 lands the real suites.
test('toolchain is wired', () => {
  assert.equal(1 + 1, 2);
});
